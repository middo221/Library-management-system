# Copy to .env.local for local development.

# Where the API lives. Leave it as a relative path to use the Vite dev proxy (and to match
# the container image, which serves the API and the SPA from the same origin).
VITE_API_BASE_URL=/api/v1

# Where the dev proxy forwards /api to.
VITE_PROXY_TARGET=http://localhost:8000
